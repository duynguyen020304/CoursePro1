// Zod Schema Library
// Barrel export for all schemas organized by domain

// Auth schemas (login, signup, password reset, etc.)
export * from './auth';

// User schemas (profile, settings, etc.)
export * from './user';

// Course schemas (course, chapter, lesson, etc.)
export * from './course';

// Order schemas (checkout, order history, cart, etc.)
export * from './order';

// Common schemas (email, password, pagination, API response wrappers)
export * from './common';

// Admin schemas (role management, etc.)
export * from './admin';
